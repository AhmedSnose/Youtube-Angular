import { SpanetoDirective } from './spaneto.directive';

describe('SpanetoDirective', () => {
  it('should create an instance', () => {
    const directive = new SpanetoDirective();
    expect(directive).toBeTruthy();
  });
});
